services:
  backend:
    build: ./packages/backend
    container_name: bun-backend
    restart: always
    ports:
      - "8080:3000"
    environment:
      - DATABASE_URL=libsql://libsql-server:8081
      # Add other env vars if needed
    depends_on:
      - libsql-server

  libsql-server:
    image: ghcr.io/tursodatabase/libsql-server:latest
    container_name: libsql-server
    restart: always
    ports:
      - "127.0.0.1:8081:8080" # We only need the HTTP port now
    volumes:
      - libsql-data:/var/lib/sqld
    environment:
      - SQLD_NODE=primary

volumes:
  libsql-data: